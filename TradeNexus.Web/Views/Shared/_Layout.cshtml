<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - TradeNexus</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.4/css/dataTables.bootstrap5.min.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet">
    <style>
        /* ══════════════════════════════════════════
           TRADENEXUS — INVENTORY INTELLIGENCE STYLE
           Light body, dark navy navbar, clean cards
        ══════════════════════════════════════════ */
        * { margin:0; padding:0; box-sizing:border-box; }

        body {
            font-family: 'Inter', 'Segoe UI', sans-serif;
            background: #eef2f7;
            min-height: 100vh;
            color: #1e293b;
        }

        /* ── Top Navbar (dark navy) ── */
        .tn-navbar {
            background: linear-gradient(135deg, #0f172a 0%, #1a2744 100%);
            padding: 0 28px;
            height: 56px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            border-bottom: 1px solid rgba(255,255,255,0.08);
            box-shadow: 0 2px 12px rgba(0,0,0,0.3);
            position: sticky; top: 0; z-index: 1000;
        }
        .tn-brand {
            display: flex; align-items: center; gap: 10px;
            text-decoration: none;
        }
        .tn-brand-icon {
            width: 32px; height: 32px;
            background: linear-gradient(135deg, #3b82f6, #1d4ed8);
            border-radius: 8px;
            display: flex; align-items: center; justify-content: center;
            font-size: 0.9rem; color: white;
        }
        .tn-brand-name {
            font-size: 1.05rem; font-weight: 800;
            color: #ffffff; letter-spacing: -0.3px;
        }
        .tn-brand-sub {
            font-size: 0.62rem; color: rgba(148,163,184,0.8);
            font-weight: 400; display: block; margin-top: -2px;
        }
        .tn-nav-right { display: flex; align-items: center; gap: 14px; }
        .tn-nav-time { font-size: 0.8rem; color: #94a3b8; font-family: 'Poppins', monospace; }
        .tn-nav-link {
            color: rgba(148,163,184,0.9); font-size: 0.78rem; font-weight: 500;
            text-decoration: none; padding: 5px 10px; border-radius: 6px;
            transition: all 0.2s;
        }
        .tn-nav-link:hover { background: rgba(255,255,255,0.08); color: #fff; }
        .tn-btn-sync {
            background: #2563eb; color: white;
            border: none; border-radius: 8px;
            padding: 6px 16px; font-size: 0.78rem; font-weight: 700;
            cursor: pointer; transition: all 0.2s;
            display: flex; align-items: center; gap: 6px;
        }
        .tn-btn-sync:hover { background: #1d4ed8; transform: translateY(-1px); box-shadow: 0 4px 12px rgba(37,99,235,0.4); }

        /* ── Page body ── */
        .tn-body { padding: 20px 24px; min-height: calc(100vh - 56px); }

        /* ── KPI Cards ── */
        .kpi-card {
            background: #ffffff;
            border-radius: 14px;
            padding: 20px 22px;
            border: 1px solid rgba(0,0,0,0.06);
            box-shadow: 0 1px 6px rgba(0,0,0,0.06), 0 4px 16px rgba(0,0,0,0.04);
            transition: all 0.25s cubic-bezier(0.4,0,0.2,1);
            position: relative;
            overflow: hidden;
            height: 100%;
        }
        .kpi-card:hover { transform: translateY(-3px); box-shadow: 0 8px 28px rgba(0,0,0,0.1); }
        .kpi-card-icon {
            position: absolute; top: 18px; right: 18px;
            width: 36px; height: 36px; border-radius: 10px;
            display: flex; align-items: center; justify-content: center;
            font-size: 1rem;
        }
        .kpi-label { font-size: 0.72rem; color: #64748b; font-weight: 600; text-transform: uppercase; letter-spacing: 0.6px; margin-bottom: 8px; }
        .kpi-value { font-size: 2rem; font-weight: 800; color: #0f172a; font-family: 'Poppins', sans-serif; line-height: 1; margin-bottom: 6px; }
        .kpi-sub   { font-size: 0.68rem; color: #94a3b8; display: flex; align-items: center; gap: 5px; }

        /* ── Section card (white panel) ── */
        .tn-panel {
            background: #ffffff;
            border-radius: 14px;
            border: 1px solid rgba(0,0,0,0.06);
            box-shadow: 0 1px 6px rgba(0,0,0,0.05), 0 4px 16px rgba(0,0,0,0.03);
            overflow: hidden;
        }
        .tn-panel-header {
            padding: 16px 20px;
            border-bottom: 1px solid #f1f5f9;
            display: flex; align-items: center; justify-content: space-between;
        }
        .tn-panel-title {
            font-size: 0.88rem; font-weight: 700; color: #0f172a;
            display: flex; align-items: center; gap: 8px;
        }
        .tn-panel-body { padding: 20px; }

        /* ── Filter tabs ── */
        .tn-tabs { display: flex; gap: 6px; flex-wrap: wrap; }
        .tn-tab {
            padding: 5px 14px; border-radius: 20px; font-size: 0.72rem;
            font-weight: 600; border: 1.5px solid #e2e8f0; cursor: pointer;
            background: white; color: #64748b; transition: all 0.2s;
        }
        .tn-tab:hover { border-color: #94a3b8; color: #334155; }
        .tn-tab.tab-active-all  { border-color: #2563eb; color: #2563eb; background: #eff6ff; }
        .tn-tab.tab-active-high { border-color: #ef4444; color: #ef4444; background: #fef2f2; }
        .tn-tab.tab-active-mid  { border-color: #f59e0b; color: #d97706; background: #fffbeb; }
        .tn-tab.tab-active-safe { border-color: #22c55e; color: #16a34a; background: #f0fdf4; }

        /* ── Client rows (table-like, Inventory style) ── */
        .client-row {
            display: grid;
            grid-template-columns: 2.5fr 1.2fr 1.2fr 1.4fr 1.4fr auto;
            align-items: center;
            padding: 14px 20px;
            border-bottom: 1px solid #f1f5f9;
            transition: background 0.15s;
            gap: 10px;
        }
        .client-row:last-child { border-bottom: none; }
        .client-row:hover { background: #f8faff; }
        .client-row-header {
            display: grid;
            grid-template-columns: 2.5fr 1.2fr 1.2fr 1.4fr 1.4fr auto;
            padding: 10px 20px;
            gap: 10px;
            background: #f8fafc;
            border-bottom: 1px solid #e2e8f0;
        }
        .col-hd { font-size: 0.62rem; font-weight: 700; color: #94a3b8; text-transform: uppercase; letter-spacing: 0.8px; }
        .cr-name { font-size: 0.85rem; font-weight: 700; color: #0f172a; }
        .cr-sub  { font-size: 0.65rem; color: #94a3b8; margin-top: 1px; }
        .cr-val  { font-size: 0.88rem; font-weight: 700; color: #0f172a; font-family: 'Poppins', sans-serif; }
        .cr-meta { font-size: 0.68rem; color: #64748b; }

        /* Risk pills */
        .risk-pill {
            display: inline-flex; align-items: center; gap: 4px;
            padding: 4px 10px; border-radius: 20px;
            font-size: 0.62rem; font-weight: 700; letter-spacing: 0.3px;
        }
        .pill-high { background: #fef2f2; color: #dc2626; border: 1px solid #fecaca; }
        .pill-mid  { background: #fffbeb; color: #d97706; border: 1px solid #fde68a; }
        .pill-safe { background: #f0fdf4; color: #16a34a; border: 1px solid #bbf7d0; }

        /* Utilization bar (inline) */
        .util-inline { display: flex; align-items: center; gap: 8px; }
        .util-track  { flex: 1; height: 5px; border-radius: 5px; background: #e2e8f0; overflow: hidden; min-width: 60px; }
        .util-fill   { height: 100%; border-radius: 5px; transition: width 1s ease; }
        .util-pct    { font-size: 0.68rem; font-weight: 700; min-width: 34px; text-align: right; }

        /* Action buttons */
        .cr-btn {
            display: inline-flex; align-items: center; gap: 4px;
            padding: 5px 12px; border-radius: 7px; font-size: 0.68rem;
            font-weight: 700; border: none; cursor: pointer; transition: all 0.2s;
            text-decoration: none; white-space: nowrap;
        }
        .btn-trades { background: #eff6ff; color: #2563eb; border: 1px solid #dbeafe; }
        .btn-trades:hover { background: #2563eb; color: white; }
        .btn-riskx  { background: #fef2f2; color: #dc2626; border: 1px solid #fecaca; }
        .btn-riskx:hover  { background: #dc2626; color: white; }

        /* ── Search ── */
        .tn-search-wrap { position: relative; }
        .tn-search {
            background: #f8fafc; border: 1.5px solid #e2e8f0;
            border-radius: 9px; padding: 7px 12px 7px 34px;
            font-size: 0.8rem; color: #334155; width: 220px;
            transition: all 0.2s;
        }
        .tn-search:focus { border-color: #93c5fd; outline: none; background: white; box-shadow: 0 0 0 3px rgba(147,197,253,0.3); }
        .tn-search::placeholder { color: #94a3b8; }
        .tn-search-icon { position: absolute; left: 10px; top: 50%; transform: translateY(-50%); color: #94a3b8; font-size: 0.72rem; }

        /* ── Charts (white panels) ── */
        .chart-card {
            background: white; border-radius: 14px;
            border: 1px solid rgba(0,0,0,0.06);
            box-shadow: 0 1px 6px rgba(0,0,0,0.05);
            padding: 18px; height: 290px;
        }
        .chart-card-title {
            font-size: 0.75rem; font-weight: 700; color: #374151;
            text-transform: uppercase; letter-spacing: 0.6px;
            margin-bottom: 14px; display: flex; align-items: center; gap: 8px;
        }

        /* ── Toast ── */
        .tn-toast {
            position: fixed; bottom: 24px; right: 24px; z-index: 9999;
            background: #0f172a; border: 1px solid rgba(255,255,255,0.1);
            border-radius: 12px; padding: 13px 18px; font-size: 0.78rem;
            color: #e2e8f0; box-shadow: 0 8px 32px rgba(0,0,0,0.3);
            display: flex; align-items: center; gap: 10px;
            transform: translateY(80px); opacity: 0;
            transition: all 0.3s cubic-bezier(0.4,0,0.2,1); max-width: 300px;
        }
        .tn-toast.show { transform: translateY(0); opacity: 1; }

        /* ── Modals ── */
        .modal-content { border-radius: 16px; border: none; box-shadow: 0 20px 60px rgba(0,0,0,0.2); overflow: hidden; }
        .modal-header  { padding: 18px 24px; border-bottom: 1px solid #f1f5f9; }
        .modal-body    { padding: 0; background: #f8fafc; }
        .modal-title   { font-size: 0.9rem; font-weight: 700; color: #0f172a; }
        .trade-mh      { background: linear-gradient(135deg, #eff6ff, #f0f9ff); }
        .risk-mh       { background: linear-gradient(135deg, #fef2f2, #fff7ed); }

        /* ── Footer ── */
        .tn-footer {
            text-align: center; padding: 16px;
            font-size: 0.68rem; color: #94a3b8;
            border-top: 1px solid #e2e8f0; background: #f8fafc;
        }

        /* ── Responsive ── */
        @@media (max-width: 768px) {
            .tn-body { padding: 12px; }
            .client-row, .client-row-header { grid-template-columns: 1fr 1fr; gap: 6px; }
        }

        /* ── Table in modal ── */
        .modal-body .table thead th { background: #f1f5f9; color: #374151; font-size:0.72rem; padding: 10px 12px; border: none; }
        .modal-body .table tbody td { font-size: 0.78rem; padding: 10px 12px; vertical-align: middle; border-color: #f1f5f9; }
        .modal-body .table tbody tr:hover { background: #f8faff; }

        /* DataTable search */
        .dataTables_wrapper .dataTables_filter input { border: 1.5px solid #e2e8f0; border-radius: 8px; padding: 5px 12px; font-size: 0.78rem; }
        .dataTables_wrapper .dataTables_filter input:focus { border-color: #93c5fd; outline: none; }
    </style>
</head>
<body>
    <!-- ── Dark Navy Top Navbar ── -->
    <nav class="tn-navbar">
        <a class="tn-brand" asp-controller="Trade" asp-action="Index">
            <div class="tn-brand-icon"><i class="fas fa-chart-line"></i></div>
            <div>
                <span class="tn-brand-name">TradeNexus</span>
                <span class="tn-brand-sub">Portfolio & Risk Management</span>
            </div>
        </a>
        <div class="tn-nav-right">
            <span class="tn-nav-time" id="navClock"></span>
            <a class="tn-nav-link" asp-controller="Trade" asp-action="Index"><i class="fas fa-home"></i> Dashboard</a>
            <a class="tn-nav-link" href="#" data-bs-toggle="modal" data-bs-target="#aboutModal"><i class="fas fa-info-circle"></i> About</a>
            <button class="tn-btn-sync" onclick="location.reload()"><i class="fas fa-sync-alt" style="font-size:0.7rem;"></i> Refresh</button>
        </div>
    </nav>

    <!-- ── Page Body ── -->
    <div class="tn-body">
        @RenderBody()
    </div>

    <!-- Toast -->
    <div class="tn-toast" id="tnToast"><span id="tnToastMsg"></span></div>

    <!-- ── Footer ── -->
    <div class="tn-footer">
        <strong>TradeNexus</strong> &nbsp;·&nbsp; Multi-Tenant B2B Brokerage Infrastructure &nbsp;·&nbsp;
        &copy; 2026 All Rights Reserved
    </div>

    <!-- About Modal -->
    <div class="modal fade" id="aboutModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title"><i class="fas fa-info-circle" style="color:#2563eb;margin-right:8px;"></i>About TradeNexus</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body" style="padding:24px; background:white;">
                    <p><strong>TradeNexus</strong> is an enterprise-grade Multi-Tenant B2B Brokerage Infrastructure Management System.</p>
                    <ul class="list-unstyled mt-3" style="font-size:0.85rem;">
                        <li style="padding:4px 0;"><i class="fas fa-check-circle" style="color:#22c55e;margin-right:8px;"></i>Real-time Trade Management</li>
                        <li style="padding:4px 0;"><i class="fas fa-check-circle" style="color:#22c55e;margin-right:8px;"></i>Advanced Python Risk Analysis Engine</li>
                        <li style="padding:4px 0;"><i class="fas fa-check-circle" style="color:#22c55e;margin-right:8px;"></i>Multi-Broker Architecture</li>
                        <li style="padding:4px 0;"><i class="fas fa-check-circle" style="color:#22c55e;margin-right:8px;"></i>SQLite (Local) / SQL Server (Remote)</li>
                        <li style="padding:4px 0;"><i class="fas fa-check-circle" style="color:#22c55e;margin-right:8px;"></i>Export &amp; Reporting</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.4/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.4/js/dataTables.bootstrap5.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        // Nav clock
        function navTick() {
            var n=new Date(), h=String(n.getHours()).padStart(2,'0'), m=String(n.getMinutes()).padStart(2,'0'), s=String(n.getSeconds()).padStart(2,'0');
            var el = document.getElementById('navClock');
            if(el) el.textContent = 'Last synced ' + h+':'+m+':'+s;
        }
        setInterval(navTick, 1000); navTick();

        // Global toast
        function showToast(msg, color) {
            var t = document.getElementById('tnToast');
            t.style.borderLeftColor = color || '#2563eb';
            document.getElementById('tnToastMsg').textContent = msg;
            t.classList.add('show');
            setTimeout(function() { t.classList.remove('show'); }, 3500);
        }

        // DataTables init
        $(document).ready(function(){
            if ($('.data-table').length) {
                $('.data-table').DataTable({ pageLength:25, order:[[0,'asc']], language:{ search:"", searchPlaceholder:"Search records..." } });
            }
        });
    </script>
    @RenderSection("Scripts", required: false)
</body>
</html>
